REQUEST HB_GT_WIN_DEFAULT

Function Main()
   ? "QAC (Quiero algo comodo !!!) Console Mode Sample"
   ? " "
   ? "Running with " + Version()
   ? " "
   ? "         and " + HB_Compiler()
   ? " "
   ? " "
   __Run( "Pause" )
Return .t.
